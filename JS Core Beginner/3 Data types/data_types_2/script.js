// Напишите функцию, которая принимает
// строку и выводит в консоль последний
// символ строки.

function lastSymbol(str){
    return str[str.length-1]
}

console.log(lastSymbol("House"))
console.log(lastSymbol("LivE"))